div.activities
  div.activity(ng-repeat="activity in activities" ng-click="logActivity()")
    div.activityBorder
      div.activityPoints
        p {{activity.points}}
          span pts.
      div.activityName {{activity.activity}}
      div.activityDesc {{activity.description}}